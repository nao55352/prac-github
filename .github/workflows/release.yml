name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: リリースを作成
        uses: softprops/action-gh-release@v1
        with:
          body: "リリースノート"
